1.作用域与作用域链

2.垃圾回收机制(Garbage Collection)
    2.1 引用计数法,标记清除法

3.闭包

4.变量提升：只存在于var，会将变量的声明而不含定义提升至其作用域开头

5.函数提升：同样的只提升声明，不提升定义，进而函数表达式的提升无效果

6.函数参数
    6.1 动态参数 arguments  伪数组
    6.2 剩余参数 ...剩余参数名  数组
    6.3 展开运算符 ...数组

7.箭头函数
    7.1 基本语法及其简化写法 (对于 =>{return {attr:value}} 返回一个对象的函数体，简写为 =>({attr:value) )
    7.2 无arguments，自身不生成this

8.数组解构
    注意点：1. 使用[]字面量开头，需在其上一行加分号
           2. 使用剩余参数解决变量少单元值多
           3. 可设默认值
           4. 可按需忽略某些值
           5. 多层次数组解构

9.对象解构
    注意点：1. 被赋值的变量必须与属性同名，但可以通过重命名语法避免变量名冲突
           2. 数组与对象的混合解构
           3. 多层次对象解构需要在次层次变量处添加次层次对象名:
           4. 解构形式参数

10. arr.forEach(function(item,index,arr){})
